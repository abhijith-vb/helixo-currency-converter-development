const clearAllDataValues = self => {
    // hxo$.each(self.data(), function (key) {
    //     const attr = `data-${key.replace(/([A-Z])/g, '-$1').toLowerCase()}`;
    //     self.removeAttr(attr);
    // });
};
export default clearAllDataValues;
